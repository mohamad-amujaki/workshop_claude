{
  "builds": [
    { "src": "api/**/*.js", "use": "@vercel/node" },
    { "src": "*.{html,css,js,json,png,svg,ico}", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "handle": "filesystem" }
  ]
}
